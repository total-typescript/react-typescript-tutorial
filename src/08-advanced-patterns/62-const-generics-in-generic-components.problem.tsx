/**
 * Have a Form component that can receive an object where
 * the keys are the different form elements, and have the
 * values inferred based on values passed in.
 */

const Form = <const TValues extends Record<string, any>>() => {};
